const NodesList = artifacts.require('./NodesList.sol');

contract('NodesList', (accounts) => {
  it('Adds a new member to the mapping', function() {
  });
  it('Removes member from mapping', function() {
  });
});
